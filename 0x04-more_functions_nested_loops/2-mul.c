#include "main.h"

/**
 * mul - mutiplies two integers
 * @a: the first number
 * @b: the second number
 *
 * Return: the mutiplication of the two numbers
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
