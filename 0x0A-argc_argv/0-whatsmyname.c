#include <stdio.h>

/**
 * main - prints out the name of the program
 * @argc: the number of arguments
 * @argc: array of string containing the command line arguments
 * Return: integer
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
